getlogin()

```
/*
 unistd.h is a built-in header file in Linux/Unix system, which contains
 function prototypes of many system call
 "uni" part in unistd stands for "UNIX",
*/
#include <unistd.h>

  printf("Welcome, %s!\n", getlogin());
```
